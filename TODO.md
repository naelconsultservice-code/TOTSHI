# TODO - Correction erreurs contact page

- [x] Lire `src/app/[locale]/contact/page.tsx`.
- [x] Identifier l’erreur JSX (balise `</a>` / `</Link>` incorrecte).
- [x] Corriger le markup pour que le lien mailto utilise une balise `<a>`.
- [ ] Vérifier que le fichier ne contient plus d’erreurs de compilation/lint (run lint/tsc) dans un terminal Windows valide.

